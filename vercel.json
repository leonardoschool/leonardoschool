{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm test:run && pnpm build",
  "crons": [
    {
      "path": "/api/cron/check-expired-contracts",
      "schedule": "0 0 * * *"
    }
  ]
}
