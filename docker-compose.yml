# Docker Compose per sviluppo locale

services:
  postgres:
    image: postgres:16-alpine
    container_name: leonardoschool_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: leonardoschool
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Redis per caching futuro (opzionale per ora)
  # redis:
  #   image: redis:7-alpine
  #   container_name: leonardoschool_redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"

volumes:
  postgres_data:
